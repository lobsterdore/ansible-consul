---

consul:
  bind_interface: eth0
  settings:
    bootstrap_expect: 1
    data_dir: "/var/lib/consul"
    enable_debug: false
    log_level: "warn"
    server: false
    start_join: []
  startopts: ""
  version: "0.6.4"
